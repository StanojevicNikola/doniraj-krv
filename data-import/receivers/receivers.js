const ids = require('../../ids/ids');

module.exports = 
[
	{
		_id: ids.receiver2Id,
		user: ids.user2Id,
		bloodGroup: ids.bloodGroup2Id,
	},
	{
		_id: ids.receiver4Id,
		user: ids.user4Id,
		bloodGroup: ids.bloodGroup4Id,
	},
]